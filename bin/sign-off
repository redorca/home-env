#!/bin/bash

[ ! -f "$1" ] && exit 5
echo "Sign $1"

if gpg --debug-level guru --verbose --clearsign -o InRelease $1; then
        echo "Create gpg file."
        if gpg -abs -o Release.gpg.xxx $1 ; then
                mv Release.gpg.xxx Release.gpg
        fi
else
        echo "Failed."
fi

