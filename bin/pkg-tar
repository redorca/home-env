#!/bin/bash

PKGDIR=nuttx-7.25
TARFILE=$PKGDIR.tar.xz

TAROPTS=" --exclude-vcs --exclude-vcs-ignores --exclude=nuttx_apps/* --exclude=debian/* --exclude=.gitignore --checkpoint=500"

CMD="tar -Jcf $TARFILE $TAROPTS $PKGDIR"
echo $CMD
$CMD


